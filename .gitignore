# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.config/
.env

# Bundle analysis output. Deliberately outside dist/spa so it is never deployed.
build-artifacts/

# wrangler pages dev local state
.wrangler/

# Screenshots and one-off measurement scripts used while working on a layout.
# The scripts are worth keeping locally and worthless in review — they capture
# page heights and viewport slices, and their output is megabytes of PNG.
research/shots/
verify*.md
