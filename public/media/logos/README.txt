Placeholder partner logos for the marquee example.
To use real logos: replace each <slug>.svg with the official logo file
(same filename, SVG preferred; PNG with transparent background also works).
Get brand-compliant assets from each partner's brand/partner portal.
Filenames map to platform names lowercased with spaces as hyphens
(e.g., "MS Dynamics" -> ms-dynamics.svg).
